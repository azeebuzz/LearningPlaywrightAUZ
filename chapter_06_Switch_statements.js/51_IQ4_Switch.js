let status = 0;

switch (status) {
    case false:
        console.log("false is matched");
        break;
    case 0:
        console.log("0 matched");
        break;
}

// Output would be 0 matched since it's strict equality
console.log(max_retries);
const max_retries = 5;

const score = 100;
score = score + 10; // This will cause an error because score is a constant

// const is used for values that should not change, and it is not hoisted like var or let. It will throw a ReferenceError if you try to access it before declaration, and it cannot be reassigned after its initial assignment.


